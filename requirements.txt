# SignalCare AI — pinned dependencies
# Install with: pip install -r requirements.txt
# Python 3.12 (Windows)

# LLM labeling
langchain==1.2.10
langchain-core==1.2.16
langchain-openai==1.1.10
openai==2.24.0

# Schema / validation
pydantic==2.12.5
pydantic-core==2.41.5

# PII redaction
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362

# Transformers / models
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
transformers==5.4.0
tokenizers==0.22.2
safetensors==0.7.0
accelerate==1.13.0

# Embeddings / domain adaptation
sentence-transformers==5.5.0
datasets==4.8.5
scikit-learn==1.9.0
scipy==1.17.1
nltk==3.9.4
xgboost==3.2.0

# Data
numpy==2.4.2
pandas==3.0.1

# Plotting
matplotlib==3.10.9
seaborn==0.13.2

# Experiment tracking (optional, dev-only — needed for
# src/train_sentiment_v2_experiments.py, v3, v4. Not installed in the
# Docker image. mlflow==3.13.0 requires pandas<3, which conflicts with the
# pandas==3.0.1 pin above, so install separately if running these scripts:
#   pip install mlflow==3.13.0

# Progress / config
tqdm==4.67.3
python-dotenv==1.2.1

# Notebooks
ipykernel==7.2.0
jupyter-client==8.8.0
jupyter-core==5.9.1

# Web app (dashboard)
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
jinja2==3.1.4
python-multipart==0.0.20
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
itsdangerous==2.2.0
ffmpeg-python==0.2.0
pytubefix>=9.0.0
